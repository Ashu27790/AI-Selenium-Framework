<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ashutosh.automation</groupId>
        <artifactId>ai-selenium-framework</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>automation-report</artifactId>

    <name>automation-report</name>

    <description>
        Reporting module for AI Selenium Framework
    </description>

    <dependencies>

        <!-- Common Module -->
        <dependency>
            <groupId>com.ashutosh.automation</groupId>
            <artifactId>automation-common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Configuration Module -->
        <dependency>
            <groupId>com.ashutosh.automation</groupId>
            <artifactId>automation-config</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Driver Module -->
        <dependency>
            <groupId>com.ashutosh.automation</groupId>
            <artifactId>automation-driver</artifactId>
            <version>${project.version}</version>
        </dependency>
        
   

        <!-- Page Module -->
        <dependency>
            <groupId>com.ashutosh.automation</groupId>
            <artifactId>automation-page</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
    <groupId>com.ashutosh.automation</groupId>
    <artifactId>automation-ai</artifactId>
    <version>${project.version}</version>
</dependency>
        

        <!-- Extent Reports -->
        <dependency>
            <groupId>com.aventstack</groupId>
            <artifactId>extentreports</artifactId>
            <version>5.1.2</version>
        </dependency>

        <!-- TestNG -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
        </dependency>

    </dependencies>

</project>